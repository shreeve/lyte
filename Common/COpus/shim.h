/* The one libopus module shared by the client decoder and Host encoder.
   pkg-config supplies an include path ending in the opus header directory
   on both Homebrew and apt installations. */
#include <opus.h>
